pipeline {
    agent any 
    stages {
        stage('Pull') { 
            steps {
                git credentialsId: '1', url: 'https://github.com/ShubhamUChoudhary/pipeliine.git'
            }
        }
        stage('Build') { 
            steps {
                
            }
        }
        stage('Deploy') { 
            steps {
                // 
            }
        }
    }
}